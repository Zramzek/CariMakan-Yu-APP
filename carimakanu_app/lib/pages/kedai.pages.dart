import 'package:flutter/material.dart';

class KedaiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kedai Page'),
      ),
      body: Center(
        child: Text('Welcome to Kedai Page!'),
      ),
    );
  }
}
